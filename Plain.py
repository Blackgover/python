# username = input('What is your name? ')
# password = input('Please enter your password ')

# length = len(password)
# pin = '*' * len(password)

# print(f'Hello {username}, your password {pin} is {length} letters long')
